<mvc:View
    controllerName="odata.project1908.controller.Detail"
    xmlns:mvc="sap.ui.core.mvc"
    displayBlock="true"
    xmlns="sap.m"
    xmlns:core="sap.ui.core">
    <Page id="detail" title="" 		
        showNavButton="true"
		navButtonPress="onBack">
        <content>
            <Input id="idDetailInput" value="detail Page"/>
            <Button text="main으로" press="onNavMain"></Button>
        </content>
        <VBox >
            <Text text="{main>/OrderID}" />
            <Text text="{main>/CustomerID}" />
            <Text text="{main>/ShipName}"/>
            <Text text="{main>/ShipAddress}"/>
            <Button id="idDetailButton" text="123" press="onRead" />
        </VBox> 

    </Page>
</mvc:View>